part of 'package:butcher_core/src/core/controller/controller.dart';

mixin QUtils on _QueenControllerImp {
  String timgeAgo(DateTime date) {
    //   // TODO :: fix time ago
    //   final _agoPeriod = DateTime.now().subtract(const Duration(hours: 1));
    //   return timeago.format(_agoPeriod, locale: Q.locale.languageCode);
    throw UnimplementedError();
  }
}
